services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-transcriptly}
      POSTGRES_USER: ${POSTGRES_USER:-transcriptly}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-transcriptly}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

  migrate:
    image: ghcr.io/liujiaqi222/transcriptly-migrate:latest
    build:
      context: .
      target: migrator
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-transcriptly}:${POSTGRES_PASSWORD:-transcriptly}@postgres:5432/${POSTGRES_DB:-transcriptly}
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  app:
    image: ghcr.io/liujiaqi222/transcriptly-app:latest
    build:
      context: .
      target: app
      args:
        DATABASE_URL: postgresql://${POSTGRES_USER:-transcriptly}:${POSTGRES_PASSWORD:-transcriptly}@postgres:5432/${POSTGRES_DB:-transcriptly}
        BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:?Set BETTER_AUTH_SECRET in .env}
        BETTER_AUTH_URL: ${BETTER_AUTH_URL:-http://localhost:3000}
        GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID:?Set GITHUB_CLIENT_ID in .env}
        GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET:?Set GITHUB_CLIENT_SECRET in .env}
        GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:?Set GOOGLE_CLIENT_ID in .env}
        GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:?Set GOOGLE_CLIENT_SECRET in .env}
        EXTENSION_ORIGINS: ${EXTENSION_ORIGINS:-chrome-extension://jkopejjjgdkkacabdhgdlploehikphai}
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-transcriptly}:${POSTGRES_PASSWORD:-transcriptly}@postgres:5432/${POSTGRES_DB:-transcriptly}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:?Set BETTER_AUTH_SECRET in .env}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL:-http://localhost:3000}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:?Set GOOGLE_CLIENT_ID in .env}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:?Set GOOGLE_CLIENT_SECRET in .env}
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID:?Set GITHUB_CLIENT_ID in .env}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET:?Set GITHUB_CLIENT_SECRET in .env}
      # The extension origin is public, pinned repo-wide (deploy.yml
      # build-arg, release.mjs EXPECTED_EXTENSION_ID); defaulting it here
      # stops an unset server .env entry from overriding the image-baked
      # value with an empty string (compose environment: always wins over
      # image ENV, even when empty).
      EXTENSION_ORIGINS: ${EXTENSION_ORIGINS:-chrome-extension://jkopejjjgdkkacabdhgdlploehikphai}
    ports:
      - "${APP_PORT:-3000}:3000"
    depends_on:
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3000/api/health/ready').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  postgres-data:
